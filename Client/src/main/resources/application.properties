# API配置
api.base.url=http://localhost:8080/api
api.connect.timeout=30
api.request.timeout=60
api.max.retries=3
api.retry.delay=500

# 缓存配置
cache.max.size=1000
cache.expire.write=5
cache.expire.access=10

# 日志配置
logging.level=INFO




















